# Environment & Secrets
.env
*.env
*credentials*.json
*secret*.json

# n8n
.n8n/
# Local import copies with real IDs/chat IDs bound (never committed — repo is public)
*.local.n8n.json

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Temp
*.log
tmp/

# Claude Code - track governance files, ignore local settings
.claude/settings.local.json
*.tmp

# Node tooling (workflow SDK). Track package.json + lockfile, ignore installed deps.
node_modules/

# Throwaway TypeScript from @n8n/workflow-sdk codegen — never committed (may carry
# real credential/chat IDs when generated from a live export). JSON is source of truth.
*.workflow.ts
sdk-scratch/
